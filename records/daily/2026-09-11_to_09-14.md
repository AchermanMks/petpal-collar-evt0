# 日报 2026-09-11 ～ 2026-09-14（四天汇总）

> 日期按北京时间。Mac mini 本机时区是 PDT，仓库里 `2026-09-10.md` 的"晚间补记"对应北京时间 9/11 上午，本文从其后接续。
> 9/12～9/14 虚拟机内的操作没有落到仓库和共享目录（无新提交、无回传日志、HTTP 服务无访问记录），下面标 **[待补]** 的由操作人补填。

## 一、总览

| 项 | 状态 |
|---|---|
| Windows 侧交付（Luatools.zip）接收与核对 | ✅ 完成，结论已入 `docs/09` §7 |
| F2（SIM 注册） | ⏳ 未通过。Windows 侧确认未执行（全程无卡）；Mac 侧三次冷启动仍 NOT READY；flash-kit v3 的 simid 诊断待烧录验证 |
| Mac 本地烧录环境（UTM + Windows 11 ARM） | ✅ 虚拟机建好、Windows 装好、共享目录生效；Luatools 与 v3 kit 是否已在虚拟机内就绪 **[待补]** |
| flash-kit v3 烧录 | **[待补]**：若已烧，需回传 `docs/10` §5 的三行日志与命令台三条回复 |
| Mac 磁盘 | ⚠️ 剩余 2.5 GB（88%），虚拟机硬盘文件实际占 31 GB，必须处理，见第四节 |

## 二、按日

### 09-11（周五）

- 收到 Windows 侧漏传的 `Luatools.zip`（Win11 机 `D:\Luatools` 整目录，282 MB）。取出 9 份日志和项目 ini，`tools/redact_ids.py` 打码后归档 `records/raw/collar-evt-001/`，未打码副本删除。
- **F2 在 Windows 侧的结论：未执行而非失败。** 所谓 3 分钟日志只有 60 s，60 s 时 `status 0`，Luatools 读到的小区信息全 0，烧录全程未插 SIM。交接表 IP_READY 秒数、首个 rsrp 留空。
- Luatools 应用日志证实烧录过程：06:16、06:17 两次「模块重启超时」，06:22:29 第三次经下载口 **COM15**（下载模式重枚举，与运行时 COM12/13 不同）成功，脚本区 58 KB / 512 KB。
- 意外收获：zip 内含出厂 V2044 固件 9/7～9/8 的运行日志，是被覆盖前唯一记录。确认 DA267 INT=GPIO20（chipid 0x13）、VBUS=WAKEUP1、电池阈值 4150/3400 mV；9/8 电池曾掉到 3014 mV，是当天"假死"的另一原因；出厂固件当时同样从未 IP_READY 并打印「拔卡」。
- 文档：`docs/09` 加 §7，`docs/06` 补三条硬件事实，`bringup_checklist` F1/F2 行与问题记录、`bringup_log.csv`、`fae_questions #9`、日报更新。提交 afc029c。
- 决定搭 UTM 路线做长期备用。共享目录放入 v3 kit 与 Luatools.zip；`docs/07` 按 v3 与 Windows 实测重写（无 BOOT 键、reset 时机、下载模式重枚举风险、电池开关）。
- 清盘：删 ubuntu x86 镜像、已装应用的安装包、DerivedData、飞书/Claude 缓存、清空废纸篓，18 GB → 34 GB。CoreSimulator 9.7 GB 与 Chrome 缓存未动。提交 84d8793。
- Windows 11 24H2 ARM64 ISO 在终端合成：Homebrew 装 aria2/cabextract/wimlib/cdrtools，chntpw 从源码编译，UUP dump 拉 zh-CN 专业版，wimlib 本地合成，约 6 分钟。产物 `~/petpal-vm-share/Windows11_24H2_ARM64_zh-CN_26100.1.ISO`（4.47 GB，基础 build 26100.1，累积更新未整合）。提交 c955c08。
- UTM 建虚拟机（QEMU 后端、aarch64、UEFI+TPM、USB 3.0 直通、共享 WebDAV）。第一次向导未保存，第二次建好；从 UEFI Shell 用 `FS0:` → `\efi\boot\bootaa64.efi` 引导安装程序；Windows 装入 40 GB 系统盘（当晚 11.4 GB）。
- 共享目录 `~/petpal-vm-share` 与内存 6 GB 在关机后设置生效。复制 Luatools.zip 到 C 盘遇 WebDAV 单文件上限（约 47 MB），给出两条路：Mac 侧起 `http://192.168.64.1:8000/` 直接下载，或放开 `WebClient\Parameters\FileSizeLimitInBytes`；改注册表后 Z: 断开，用 `net use Z: \\localhost@9843\DavWWWRoot` 或重启重连。

### 09-12（周六）、09-13（周日）

- 仓库、共享目录、HTTP 服务均无活动记录。**[待补]** 虚拟机内是否完成：Luatools.zip 解压运行、v3 kit 解压与 `config.lua`、`petpal-evt-v3` 项目、USB 直通、烧录结果、重枚举时虚拟机是否丢设备。

### 09-14（周一）

- 09:37（北京时间）虚拟机重新启动，一直在运行；板子未在 Mac 上枚举（已直通或已拔）。
- 发现 Mac 磁盘剩余 2.5 GB：虚拟机 qcow2 从 9/11 的 11.4 GB 涨到实际 31 GB。原因 **[待补]**（Windows Update 下载 24H2 累积更新、休眠文件与页面文件、或大文件重复复制到 C 盘都可能）。
- 本日报。

## 三、测试进展（TP / 设备 / 结果）

- 设备 collar-evt-001（台账 dev-blmq-01，Air780EGH_A11 / EC718HM，V2030_1 + evt0.0.1）。
- F0 ✅、F1 ✅（Windows 侧 9/9，日志已归档打码）。
- F2 ⏳：无卡。待 v3 的 `simid` 自动扫槽诊断行；换卡对比未做 **[待补]**。
- F3～F10 未开始；无 SIM 期间的顺序 F5 → F6 → F4 → F10 依赖 v3 的 USB 命令台，v3 烧录状态 **[待补]**。

## 四、阻塞与需要决策

1. **磁盘（今天必须做）**：2.5 GB 剩余下 macOS 和虚拟机都会不稳。建议顺序：(a) 虚拟机里看 C 盘占用，删掉重复的 zip、清 Windows Update 缓存（磁盘清理 → 清理系统文件），关休眠 `powercfg /h off`；(b) UTM 里弹出安装 ISO 后删除 Mac 上的 ISO 文件（4.5 GB，需要时可重新合成）；(c) CoreSimulator 9.7 GB 是否删除，需要决定。
2. **v3 是否已烧**：没烧则按 `docs/07` 第 6 步做，重点记录下载模式重枚举时 UTM 的 USB 表现。
3. **SIM**：仍无卡。换卡对比与 FAE 问题 #9（BLMQ 卡座接 SIM1 还是 SIM2）未推进。
4. `config.lua` 的 device_id（collar-evt-001）与台账编号（dev-blmq-01）不一致，下次烧录前定一个。

## 五、采购/到货

- 无新增。备选开发板（`docs/11`）未下单。

## 六、下一步

1. 处理磁盘，把剩余拉回 15 GB 以上。
2. 补齐 9/12～9/14 虚拟机内的记录：烧录日志（先 `redact_ids.py` 打码）放 `records/raw/collar-evt-001/`，问题记入 `bringup_checklist.md`。
3. v3 烧好后在 Mac 上 `tools/usb_log.py --auto --device collar-evt-001 --tag F2` 看 `simid`/`iccid` 诊断行，然后 `usb_cmd.py` 按 F5 → F6 → F4 → F10 逐项开。
4. 烧录完成后关掉 Mac 上的 HTTP 服务（`pkill -f "http.server 8000"`）。
