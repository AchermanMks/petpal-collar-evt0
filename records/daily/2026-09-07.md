# 日报 2026-09-07

## 今日产出
- 建立 `petpal-collar-evt0` 仓库（2 次提交：项目初始化、范围调整为板级功能调试）。
- 板级调试方案 `docs/05`、Air8201G 硬件/API 备忘 `docs/06`、模块化调试固件 `firmware/wearable-evt0/`（9 个 lua 文件语法检查通过）。
- 合宙官方 Air8201 demo 与出厂工程副本入库 `firmware/reference/`。
- `tools/selftest.py` 全部 PASS（telemetry_validator / ack_checker / power_analyzer / gnss_accuracy）。
- 下载 `3D_Air8201G_v1.5.step`（Downloads，尚未放入 `hardware/ref/`）。
- F0 决策：Mac mini 上 UTM + Windows 11 ARM 烧录；板子确认为 BLMQ。已装 UTM 4.7.5、CrystalFetch 2.2.0，下载 Luatools_v3、底层固件 V2030（最新）与 V2016 到 `~/petpal-vm-share/`；步骤写在 `docs/07_F0_MAC_UTM_FLASHING.md`。

## 测试进展（TP / 设备 / 结果）
- F0 烧录环境：未开始。Mac mini 上未检测到模组 USB（`/dev/cu.*` 只有蓝牙/调试口；USB 树上只有鼠标、键盘、LAN 的集线器）。
- F1～F10：未开始，等待 F0。

## 阻塞与需要决策
- **磁盘只剩约 23 GB**，Windows 11 ARM 虚拟机至少要 25～30 GB。需要清理：Ubuntu ISO 6.1 GB、废纸篓 2.7 GB、iOS 模拟器 9.6 GB、DerivedData 2.9 GB、应用缓存约 3.9 GB（清单见 docs/07）。
- 2026-09-08 更新：烧录改到另一台 Windows 电脑做（由那边的 Claude 执行），交接单 `docs/08_WINDOWS_FLASH_HANDOFF.md`，文件包 `~/Desktop/petpal-flash-kit.zip`。Mac 虚拟机方案保留为备用，磁盘清理暂不急。
- 风险：Windows 11 ARM 对 Air780EGH USB CDC 的枚举未验证；Luatools 走 x86 转译。
- 底层固件后缀（`_1`/`_10`/`_101`…）含义待在 Luatools 里确认，先用 `_1`。

## 采购/到货
- 无新增。

## 明日计划
- 清磁盘到 ≥ 30 GB；CrystalFetch 出 ISO；UTM 建 VM 装 Windows；装 Luatools（F0）。
- BLMQ 接 Mac，确认 `/dev/cu.usbmodem*` 出现并记 VID/PID；直通给 VM，烧 V2030_1 + 脚本，看到 F1 日志。
- 填 `config.lua`（只开 mqtt），跑 F1→F2→F3。
