# 日报 2026-09-10

## 今日产出
- 收到 Windows 烧录侧交付报告，归档为 `docs/09_WINDOWS_FLASH_REPORT.md`（正文原样）。
- 据报告更新 `docs/05`（烧录路线、已知坑）、`docs/06`（BLMQ 板按键/供电/USB 实测结论）、`docs/08`（勘误：无 BOOT 键、缺库、清分区）。
- 补齐 `firmware/wearable-evt0/libs/exgnss.lua` + `lbsLoc2.lua`（LuatOS master，2026-09-10 拉取），烧录清单同步更新。
- 新增 `tools/redact_ids.py`：对 trace 日志中的 IMEI/ICCID/IMSI 打码，用于回传/归档前处理。
- 台账：`bringup_checklist.md`（F0/F1 ✅，F2 ⏳，问题记录 7 条）、`bringup_log.csv` 首行、`device_ledger.csv`（dev-blmq-01 进入 bringup）、`risk_register.csv` +2。

- 下半天：板子接回 Mac。`tools/usb_log.py` 直读 USB 日志口（13 口，115200 + 主机轮询写），不再依赖 Luatools；`tools/usb_cmd.py` + `console_app.lua` 让功能开关经 USB 写 KV 覆盖；`tools/luacheck.py` 语法检查全部通过；flash-kit v3 打包到桌面。

## 补记（下午，Luatools.zip 到手）
- Windows 侧漏传的 `Luatools.zip`（`D:\Luatools` 整目录）已到 `~/Downloads`。从中取出 9 份日志 + 项目 ini，`tools/redact_ids.py` 打码后归档到 `records/raw/collar-evt-001/`（清单见 `docs/09` §7.5），未打码的解压副本已删除。
- **F2 在 Windows 侧的最终结论：未执行**。所谓 3 分钟日志实际只有 60 s，60 s 时 `status 0`，Luatools 小区信息全 0，烧录全程没插 SIM；交接表 IP_READY/rsrp 留空。`docs/09` 加 §7，`bringup_checklist` F1/F2 行与问题记录、`bringup_log.csv` 已更新。
- Luatools 应用日志证实：06:16、06:17 两次「模块重启超时」，06:22:29 第三次经 **COM15**（下载模式口）成功，脚本区 58 KB / 512 KB，`Burnbatch OK`。
- 意外收获：zip 内有出厂 V2044 固件 9/7～9/8 的运行日志（唯一记录）。确认 DA267 INT=GPIO20（chipid 0x13）、VBUS=WAKEUP1、电池阈值 4150/3400 mV；9/8 电池曾掉到 3014 mV，是当天“假死”的另一原因。出厂固件当时同样读不到卡。`docs/06`、`fae_questions #9` 已补。
- flash-kit v3 尚未烧（zip 里的项目仍是第一版 kit），`docs/10` 交接单仍有效。

## 晚间补记（UTM 路线准备）
- 决定把 UTM 路线搭起来做长期备用。共享目录 `~/petpal-vm-share` 放入 `petpal-flash-kit-v3.zip` 与 `luatools/Luatools.zip`（Windows 机整目录，免联网自解压）。
- 清盘：删 ubuntu x86 镜像、已装应用的安装包、DerivedData、飞书/Claude 缓存、清空废纸篓，18 GB → 34 GB；CoreSimulator 与 Chrome 缓存未动。
- Windows 11 24H2 ARM64 ISO 在终端里合成（UUP dump + Homebrew 工具 + 自编译 chntpw），产物 `~/petpal-vm-share/Windows11_24H2_ARM64_zh-CN_26100.1.ISO`，4.47 GB，剩余磁盘 29 GB。`docs/07` 按此更新。
- 剩下的都要在 Mac 屏幕前做：UTM 建虚拟机（QEMU 后端）、装系统、解压 Luatools.zip、直通 USB 烧 v3；重点观察下载模式重枚举时 VM 会不会丢设备。

## 测试进展（TP / 设备 / 结果）
- 2026-09-09（Windows 侧）：V2030_1 + petpal 脚本烧录成功；F1 通过（`petpal_evt0 001.000.001 collar-evt-001 evt0.0.1`，cold boot，hmeta Air780EGH A11）。
- F2：烧录时未插 SIM；Mac 上断电重插 SIM 后三次冷启动仍 `+CPIN: NOT READY`、`status 0`（raw/collar-evt-001/log_F2_*.txt）。固件加 `mobile.simid(2)` 待验证。

## 阻塞与需要决策
- 等 Windows 侧回传：`trace_2026-09-09_062459.txt`（先用 `tools/redact_ids.py` 打码）与交接表数值（IP_READY 秒数、首个 rsrp、VID/PID）。
- config.lua 里 device_id 填的是 collar-evt-001，但该板在台账里是 dev-blmq-01；下次烧录前定一个。
- 出厂 V2044 固件已覆盖、无备份。

## 采购/到货
- 无新增。

## 明日计划
- Windows 烧 flash-kit v3（桌面 `petpal-flash-kit-v3.zip`），回 Mac 用 usb_cmd 按 F5 → F6 → F4 → F10 逐项开。
- 换一张卡对比 SIM；看 v3 的 simid 诊断行。
- 板子接回 Mac：拨动开关置通，`python3 tools/serial_log.py --list` 找 soc log 口，抓 F2 日志到 `records/raw/collar-evt-001/`。
- F2 通过后填 `bringup_log.csv` 的 sim_registered_s / rsrp_dbm；拿到 MQTT 参数再开 F3。
